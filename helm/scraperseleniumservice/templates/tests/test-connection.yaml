apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scraperseleniumservice.fullname" . }}-test-connection"
  labels:
    {{- include "scraperseleniumservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scraperseleniumservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
